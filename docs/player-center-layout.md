# 玩家中心布局（v3.16.5）

`/portal` 沿用官网的墨绿、米白、朱砂和仙山背景，桌面为修士角色侧栏与内容区，手机可展开角色选择。左侧只列角色，不列宠物；选中修士后，在右侧「灵宠」页切换该角色对应的宠物。仅有宠物的旧角色仍可通过其用户 ID 选择。

## 功能位置

| 入口 | 内容 |
| --- | --- |
| 总览 | 修士立绘与六件装备、修士属性、体力、战力构成、四种资源、活动冷却 |
| 灵宠 | 档案、升级/进化、宠物定制、自动助手 |
| 坐骑 | 全部坐骑、定制资格、新建与更换外观、审核状态 |
| 背包 | 道具搜索、每页10项、道具使用、神器佩戴、秘技参悟 |
| 兑换 | 当前角色的卡密兑换与结果 |

绑定角色、账号密码、退出、网页游玩、反馈与协议入口仍保留。分区切换保留表单输入；背包分页不会复制道具对象，数量输入仍属于原道具。切换角色重新加载对应档案，并重置背包筛选与页码。

旧响应通过请求序号隔离，避免快速切换角色时覆盖当前档案。未绑定、纯修士、加载失败状态都有明确提示。修复无宠物角色缩略图使用不存在的 `blankImg` 局部变量的问题。

## 修士立绘与装备

总览复用群聊修士图的职业、性别立绘与装备素材，中央全身立绘，左右各三件装备。每个装备位显示实际等级、按职业与进阶生成的名称、觉醒词条；未觉醒时显示明确提示。装备位固定为武器、法衣、灵印、灵冠、灵靴、玉佩，具体名称随职业变化。手机仍保持三列，完整展示立绘。

图片通过独立静态地址加载，优先使用已有 WebP；接口只返回图片地址，不内嵌图片。数据读取不修改角色存档。

## 验收

- Python 项目语法检查、玩家中心内嵌 JavaScript 语法检查通过。
- 核对模板调整前后的操作事件：原有事件处理入口无遗漏；后端接口、认证与其它页面代码保持原样。
- 本地合成账号验证桌面、390px 和 320px 手机布局；五个功能页均无整页横向溢出。
- 验证背包 26 项按 10/10/6 分页、跨页名称搜索和对应的「参悟」操作。
- 验证坐骑定制、绑定弹窗、未绑定提示、纯修士档案、加载失败重试，以及延迟旧请求下的角色切换。
- 预览仅使用合成数据，未进行真实角色写操作。
- 修士装备的四项回归测试通过，覆盖四职业男女素材、六槽顺序、等级/进阶/词条、旧角色默认值及真实玩家摘要接口。
- 立绘布局在桌面、390px 和 320px 手机视口下检查通过，七张图片均加载，页面无横向溢出。

本次涉及 `petpark/portal.py` 与装备摘要模块，服务器同步后需要框架冷重启才能生效。
